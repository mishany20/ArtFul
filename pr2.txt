#include<iostream>
#include <conio.h>
using namespace std;
void main()
{
 int s,s1,a,b;
 cin>>a>>b;
 s=a*b;
 s1=a*b;
 if (s>s1)
   {
     cout<<s;
   }
    else
   {
     cout<<s1;
   }
system(pause);
}